# Milestones

- Repository separation baseline — frontend history preserved, Flutter promoted
  to repository root, `fvp` embedded, core artifacts pinned and packaged smoke
  verified.
- Frontend planning baseline — active on `codex/repository-governance`.

Pre-split product history remains available through Git history and the archived
`LLPlayerNext` repository; it is not copied into live app planning.
