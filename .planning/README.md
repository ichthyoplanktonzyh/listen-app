# listen-app Planning

这里是 `listen-app` 的当前项目记忆，只维护 Flutter 产品、UI/UX、客户端状态、
兼容层和最终 app assembly 的事实。

## 新会话读取顺序

1. `STATE.md`
2. `PROJECT.md`
3. `MAINTENANCE.md`
4. `codebase/ARCHITECTURE.md`
5. `codebase/STRUCTURE.md`
6. `codebase/TESTING.md`
7. 当前 active phase

`listen-core` 维护独立 planning。此仓只通过 core repository、commit、release、
contract/runtime version、artifact SHA 和 issue/PR 链接引用后端事实。
